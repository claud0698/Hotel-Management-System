# Environment Configuration Template
# Copy this file to .env and fill in your values

# ===== Application Settings =====
FLASK_ENV=production
DEBUG=False
PORT=8080

# ===== Security =====
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-generate-a-random-string

# ===== Database Configuration =====

# For Local Development (SQLite)
DATABASE_URL=sqlite:///./kos.db

# For Cloud SQL PostgreSQL (Production)
# Option 1: Using Unix socket (recommended for Cloud Run)
# DATABASE_URL=postgresql+psycopg2://username:password@/database?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME

# Option 2: Using TCP connection
# DATABASE_URL=postgresql+psycopg2://username:password@INSTANCE_IP:5432/database

# For External PostgreSQL (Supabase, Neon, etc.)
# DATABASE_URL=postgresql+psycopg2://username:password@host:5432/database

# ===== CORS Configuration =====
# For development (allow all)
CORS_ORIGINS=*

# For production (specify your frontend URLs)
# CORS_ORIGINS=https://your-frontend.vercel.app,https://your-domain.com

# ===== JWT Authentication =====
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===== Cloud SQL Connection (for Cloud Run) =====
# CLOUD_SQL_CONNECTION_NAME=PROJECT_ID:REGION:INSTANCE_NAME
# DB_USER=your-db-username
# DB_PASSWORD=your-db-password
# DB_NAME=kos_db
